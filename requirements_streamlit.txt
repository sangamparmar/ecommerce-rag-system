# Streamlit Cloud Compatible Requirements
# This version works without ChromaDB to avoid SQLite issues

streamlit==1.28.1
pandas==2.0.3
google-generativeai==0.3.2
python-dotenv==1.0.0
transformers==4.35.2
numpy==1.24.3
requests==2.31.0
typing-extensions==4.8.0
scikit-learn==1.3.2

# Note: ChromaDB removed due to SQLite version conflicts on Streamlit Cloud
# App will use fallback text-based search instead of vector search
